# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# The supervisor scratchpad is runtime-only (its own .gitignore keeps README).
# Runtime backlog items, agenda contents, and escalation records ARE checked
# in by design — they are the harness's durable state.
